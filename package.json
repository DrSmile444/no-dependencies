{
  "name": "no-dependencies",
  "version": "1.3.1",
  "description": "Allows you to publish packages without dependencies. Usefull when you publish a built bundle and don't want to install dependencies",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "url": "https://github.com/DrSmile444/no-dependencies"
  },
  "keywords": [
    "dependencies",
    "dev dependencies",
    "publish",
    "npm publish",
    "npmjs",
    "packages",
    "no-dependecies"
  ],
  "author": "Dmytro Vakulenko",
  "license": "MIT",
  "dependencies": {
    "commander": "^6.0.0",
    "update-notifier": "^4.1.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
